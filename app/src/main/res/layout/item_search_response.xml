<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
            <variable
                name="citiesEntity"
                type="com.muhammetkdr.weatherapp.domain.entity.cities.CitiesEntity" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_marginStart="10dp"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="2dp"
            android:text="@{citiesEntity.cityName}"
            android:textSize="20sp"
            tools:text="DENİZLİ" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_marginStart="4dp"
            android:orientation="horizontal">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/itemLatitude"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{citiesEntity.latitude}"
                android:layout_margin="4dp"
                android:textSize="14sp"
                tools:text="36,2321" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/itemLongitude"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="4dp"
                android:text="@{citiesEntity.longitude}"
                android:textSize="14sp"
                tools:text="20.1232" />
        </LinearLayout>

    </LinearLayout>
</layout>